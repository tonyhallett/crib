/* THIS (.ts) FILE IS GENERATED BY TypedSignalR.Client.TypeScript */
/* eslint-disable */
/* tslint:disable */
import { HubConnection, IStreamResult, Subject } from '@microsoft/signalr';
import { ICribHub, ICribClient } from './AzureFunctionApp';


// components

export type Disposable = {
    dispose(): void;
}

export type HubProxyFactory<T> = {
    createHubProxy(connection: HubConnection): T;
}

export type ReceiverRegister<T> = {
    register(connection: HubConnection, receiver: T): Disposable;
}

type ReceiverMethod = {
    methodName: string,
    method: (...args: any[]) => void
}

class ReceiverMethodSubscription implements Disposable {

    public constructor(
        private connection: HubConnection,
        private receiverMethod: ReceiverMethod[]) {
    }

    public readonly dispose = () => {
        for (const it of this.receiverMethod) {
            this.connection.off(it.methodName, it.method);
        }
    }
}

// API

export type HubProxyFactoryProvider = {
    (hubType: "ICribHub"): HubProxyFactory<ICribHub>;
}

export const getHubProxyFactory = ((hubType: string) => {
    if(hubType === "ICribHub") {
        return ICribHub_HubProxyFactory.Instance;
    }
}) as HubProxyFactoryProvider;

export type ReceiverRegisterProvider = {
    (receiverType: "ICribClient"): ReceiverRegister<ICribClient>;
}

export const getReceiverRegister = ((receiverType: string) => {
    if(receiverType === "ICribClient") {
        return ICribClient_Binder.Instance;
    }
}) as ReceiverRegisterProvider;

// HubProxy

class ICribHub_HubProxyFactory implements HubProxyFactory<ICribHub> {
    public static Instance = new ICribHub_HubProxyFactory();

    private constructor() {
    }

    public readonly createHubProxy = (connection: HubConnection): ICribHub => {
        return new ICribHub_HubProxy(connection);
    }
}

class ICribHub_HubProxy implements ICribHub {

    public constructor(private connection: HubConnection) {
    }

    public readonly calledFromClient = async (intArg: number, stringArg: string): Promise<void> => {
        return await this.connection.invoke("CalledFromClient", intArg, stringArg);
    }
}


// Receiver

class ICribClient_Binder implements ReceiverRegister<ICribClient> {

    public static Instance = new ICribClient_Binder();

    private constructor() {
    }

    public readonly register = (connection: HubConnection, receiver: ICribClient): Disposable => {

        const __calledFromServer = (...args: [string, number]) => receiver.calledFromServer(...args);

        connection.on("CalledFromServer", __calledFromServer);

        const methodList: ReceiverMethod[] = [
            { methodName: "CalledFromServer", method: __calledFromServer }
        ]

        return new ReceiverMethodSubscription(connection, methodList);
    }
}

